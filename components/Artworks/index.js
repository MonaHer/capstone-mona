import { useState } from "react";
import ArtworksPreview from "../ArtworksPreview/index.js";

export default function Artworks({ artworks }) {
  return;
  /* <>
      <ul>
        {artworks.map((artwork) => (
          <li key={artwork.id}>
            <ArtworksPreview image={artwork.image_thumbnail} />
          </li>
        ))}
      </ul>
      <p>test</p>
    </>*/
}
